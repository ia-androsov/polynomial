from polynomial import *
import random
from random import randint
import argparse


def main():
    """
    lis = []
    for k in range(30):
        sign = randint(0, 1) * 2 - 1
        lis.append(random.random() * randint(1, 10000) * sign)
    p1 = Polynomial(lis)
    print(p1)
    p2 = RealPolynomial(p1)
    root = p2.find_root()
    print(root)
    print(p1(root))
    """

    return


if __name__ == "__main__":
    main()
